// 빈도수 세기 패턴 (Frequency Counting Pattern)


// 2개의 배열을 가지는 same이라는 함수를 작성하십시오. 

// 배열의 모든 값이 두 번째 배열에 해당하는 값을 가지면 참을 반환해야 합니다.

// 따라서 첫 번째 배열에는 여러 값이 있는데 두 번째 배열의 값이 정확히 동일하지만

// 제곱되어 있는지 알고자 합니다.

// 하지만 순서는 상관 없으니 동일할 필요는 없고 그저 제곱만 하면 됩니다.

// 섞일 수 있지만 값의 빈도는 동일해야 합니다.